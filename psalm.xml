<?xml version="1.0"?>
<psalm
    errorLevel="8"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="false"
    findUnusedCode="false"
    runTaintAnalysis="true"
    errorBaseline="psalm-baseline.xml"
>
    <projectFiles>
        <directory name="app" />
        <directory name="plugins" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <stubs>
        <file name="psalm-stubs.php" />
    </stubs>

    <issueHandlers>
        <!-- PHPStan owns general type/code checking at level 8. Psalm here is
             focused on taint/security analysis (XSS, SQLi, shell, SSRF, etc.).
             Error level is lenient so type noise does not bury taint findings. -->
    </issueHandlers>
</psalm>
