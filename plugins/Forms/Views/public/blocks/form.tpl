<div class="pv-block">
    <div class="pv-form-block">
        {% if title %}
        <h6 class="pv-block-title">{{ title }}</h6>
        {% endif %}
        {% if content %}
        <div class="pv-block-content">{! content !}</div>
        {% endif %}
    </div>
</div>
