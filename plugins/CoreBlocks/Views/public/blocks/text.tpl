<div class="block">
    <div class="block-text">
        {% if title %}
        <h6 class="block-title">{{ title }}</h6>
        {% endif %}
        {% if content %}
        <div class="block-content">{{ content }}</div>
        {% endif %}
    </div>
</div>
